# Simply Save Voice SDK — model pipeline
# Python 3.10+, 32GB RAM, NVIDIA GPU 16GB VRAM recommended

# HuggingFace (use HF_TOKEN; no hardcoding)
huggingface_hub>=0.20.0

# Core ML
torch>=2.1.0
transformers>=4.36.0
sentencepiece>=0.1.99
onnxscript>=0.1.0
optimum[onnxruntime]>=1.16.0
onnxruntime>=1.17.0

# Data & training
pandas>=2.0.0
openpyxl>=3.1.0
scikit-learn>=1.3.0

# Intent classifier (MobileBERT fine-tune)
datasets>=2.14.0

# TFLite export
tf2onnx>=1.15.0
tensorflow>=2.14.0

# Coqui TTS (optional, per-language)
# TTS>=0.22.0

# NeMo (optional, for FastPitch/HiFiGAN)
# nemo_toolkit[asr]>=1.22.0
